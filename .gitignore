# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local
.claude/worktrees/

# Research data caches — refetchable, and nvda_optc.json alone is 3.1MB of option
# marks. The scripts repull anything missing (which is how today's run rebuilt
# nvda_hist.json), so the repo carries the harness, not the data.
research/**/*.json
deno.lock
